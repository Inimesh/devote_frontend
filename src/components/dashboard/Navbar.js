import React from 'react'

const Navbar = () => {
  /* use a form withing useState to set the state of the component.
  Depending on this state the main window will render a different component*/
  return (
    <div>
      {/* Tab to display transactions rounding up in main window */}
      {/* Tab for displaying money sent (end of each day) */}
      {/* Tab for setting up new pots */}
      {/* Tab for displaying registered paying accounts */}
      {/* Tab for account settings*/}
    </div>
  )
}

export default Navbar